Botón circular para un solo icono (Lucide), con label accesible obligatorio.

```jsx
<IconButton label="Cerrar" variant="soft"><X size={20} /></IconButton>
```

Variantes: `soft` (lila), `accent` (lavanda), `ghost`. Diámetro por defecto 44px.
